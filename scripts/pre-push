#!/bin/bash

# pre-push hook for WeChat draft publishing

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

cd "$PROJECT_ROOT"

# Run publish script in hook mode and propagate the exit code.
node "$SCRIPT_DIR/publish-wechat.js" --hook
exit $?
